apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sso76-chart.fullname" . }}-test-connection"
  labels:
    {{- include "sso76-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sso76-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
